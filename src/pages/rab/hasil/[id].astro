---
export const prerender = false;

// Halaman ini sudah tidak aktif — sistem tidak lagi menyimpan kalkulasi ke database.
// Redirect semua akses ke halaman kalkulator utama.
return Astro.redirect('/kalkulator-rab', 301);
---
